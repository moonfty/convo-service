version: '3.9'

services:
    convo-service:
        image: convo-service
        build: .
        ports:
            - '5000:5000'
        # volumes:
        #   - './node_modules:/app/node_modules'
        #   - './src:/app/src'
        restart: on-failure
        # logging: *default-logging
        # logging: *default-logging
